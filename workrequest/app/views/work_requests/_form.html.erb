<%= form_for(@work_request) do |f| %>
  <% if @work_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_request.errors.count, "error") %> prohibited this work_request from being saved:</h2>

      <ul>
      <% @work_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :businessunit_id %><br />
    <%= collection_select(:workrequest, :businessunit_id, BusinessUnit.all, :id, :title) %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= collection_select(:workrequest, :category_id, Category.all,:id,:title) %>
  </div>
  <div class="field">
    <%= f.label :status_id %><br />
    <%= collection_select(:workrequest, :status_id, Status.all,:id,:title) %>
  </div>
  <div class="field">
    <%= f.label :plant_id %><br />
    <%= collection_select(:workrequest, :plant_id, Plant.all,:id,:title) %>
  </div>
  <div class="field">
    <%= f.label :area_id %><br />
    <%= collection_select(:workrequest, :area_id, Area.all,:id,:title) %>
    
  </div>
  <div class="field">
    <%= f.label :requestors %><br />
    <%= f.text_field :requestors %>
  </div>
  <div class="field">
    <%= f.label :queue %><br />
    <%= f.text_field :queue %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
