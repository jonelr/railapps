<%= form_for(@lookup) do |f| %>
  <% if @lookup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup.errors.count, "error") %> prohibited this lookup from being saved:</h2>

      <ul>
      <% @lookup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lookup_name %><br />
    <%= f.text_field :lookup_name %>
  </div>
  <div class="field">
    <%= f.label :lookup_key %><br />
    <%= f.text_field :lookup_key %>
  </div>
  <div class="field">
    <%= f.label :lookup_value %><br />
    <%= f.text_field :lookup_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
